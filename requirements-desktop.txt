# Прослушивание табов в настольном приложении.
#
# Нужно ТОЛЬКО для окна на компьютере. Серверу не требуется: в вебе звук
# синтезируется прямо в браузере через Web Audio, и лишний пакет там
# только мешает -- у pygame нет готовой сборки под Python 3.14, и на
# свежей Ubuntu установка пыталась бы собрать его из исходников.

pygame>=2.6
