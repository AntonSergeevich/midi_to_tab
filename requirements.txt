# Ядро: чтение MIDI, запись Guitar Pro, прослушивание.
# Этого достаточно для работы с MIDI-файлами.
pretty-midi>=0.2.10
PyGuitarPro>=0.10
pygame>=2.6          # MIDI-выход для прослушивания табов

# Окно приложения использует tkinter -- он входит в стандартную поставку
# Python для Windows, ставить отдельно ничего не нужно.

# Распознавание аудио  -> install_audio.bat      (requirements-audio.txt)
# Разделение на дорожки -> install_separation.bat (requirements-separation.txt)
