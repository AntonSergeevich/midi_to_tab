# Ядро: чтение MIDI и запись Guitar Pro.
# Этого достаточно и для веб-сервиса, и для работы с MIDI в настольном окне.
pretty-midi>=0.2.10
PyGuitarPro>=0.10

# Окно приложения использует tkinter -- он входит в стандартную поставку
# Python для Windows, ставить отдельно ничего не нужно.

# Прослушивание в НАСТОЛЬНОМ приложении -> requirements-desktop.txt
#   (серверу не нужно: веб-плеер синтезирует звук прямо в браузере)
# Распознавание аудио    -> install_audio.bat      (requirements-audio.txt)
# Разделение на партии   -> install_separation.bat (requirements-separation.txt)
# Текст песен            -> install_lyrics.bat     (requirements-lyrics.txt)
# Веб-приложение         -> requirements-web.txt
