# Ядро: чтение MIDI и запись Guitar Pro. Этого достаточно для работы с MIDI.
pretty-midi>=0.2.10
PyGuitarPro>=0.10

# Окно приложения использует tkinter -- он входит в стандартную поставку
# Python для Windows, ставить отдельно ничего не нужно.

# Распознавание аудио (стем -> MIDI) ставится ОТДЕЛЬНО, см. requirements-audio.txt
# или install_audio.bat. Причина: в метаданных basic-pitch жёстко прописан
# tensorflow, которого нет под Python 3.13, хотя на ONNX-бэкенде он не нужен.
