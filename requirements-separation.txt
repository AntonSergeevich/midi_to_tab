# Разделение готового трека на дорожки инструментов.
#
# ВНИМАНИЕ: сначала поставьте PyTorch БЕЗ CUDA, иначе pip скачает около
# двух гигабайт драйверов для видеокарты, которой на сервере нет:
#
#     pip install torch --index-url https://download.pytorch.org/whl/cpu
#     pip install -r requirements-separation.txt
#
# Или одной командой -- install_separation.sh рядом делает то же самое
# и заранее проверяет свободное место.
#
# Demucs (Meta, MIT) с моделью htdemucs_6s выделяет шесть дорожек, включая
# ОТДЕЛЬНУЮ ГИТАРУ. Для качества табов это важнее любых настроек: чистый
# гитарный стем распознаётся несравнимо точнее, чем гитара внутри микса.
#
# Веса модели скачиваются при первом запуске -- нужен интернет, дальше
# работает офлайн.

demucs>=4.0
