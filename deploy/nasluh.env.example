# Переменные окружения НАСЛУХ.
# Копируется в /opt/nasluh/nasluh.env и правится там.
# Файл содержит секреты -- права 600, владелец root.

# --- обязательное ---
MIDI2TAB_DATA=/opt/nasluh/data
# Ключ подписи куки. Без него он генерируется заново при каждом
# перезапуске: у всех пользователей слетает счётчик бесплатных песен.
# Сгенерировать:  openssl rand -hex 32
MIDI2TAB_SECRET=ЗАМЕНИТЕ_НА_СЛУЧАЙНУЮ_СТРОКУ

# Ключ входа в админку. Без него страница /admin недоступна вообще.
MIDI2TAB_ADMIN_KEY=ЗАМЕНИТЕ_НА_ДРУГУЮ_СЛУЧАЙНУЮ_СТРОКУ

# --- пределы ---
MIDI2TAB_MAX_MB=60
# Должно быть не больше client_max_body_size в nginx, иначе nginx
# отрежет загрузку раньше, чем приложение успеет объяснить причину.

# --- уборка диска ---
NASLUH_KEEP_UPLOADS_DAYS=14
NASLUH_KEEP_STEMS_DAYS=14
NASLUH_LOW_DISK_RATIO=0.12

# --- оплата (заполняется, когда подключите магазин) ---
# PAYMENT_PROVIDER=getplatinum
# --- GetPlatinum, API версии 2 -------------------------------------------
# Терминал уже прописан в коде, нужен только секретный ключ из кабинета.
PAYMENT_PROVIDER=getplatinum
GETPLATINUM_TERMINAL=153777
# GETPLATINUM_SECRET_KEY=
#
# Адрес уведомлений в кабинете GetPlatinum:
#     https://naslux.ru/api/webhook/getplatinum
#
# Состав строки для контрольной подписи в версии 2 сверяется с
# документацией сервиса. Если он окажется другим -- правится здесь, без
# выкладки новой версии:
# GETPLATINUM_SIGN_FIELDS=terminal,order_id,amount,currency
# GETPLATINUM_CALLBACK_SIGN_FIELDS=terminal,order_id,amount,status
# GETPLATINUM_SIGN_FIELD=signature
# GETPLATINUM_API_URL=https://api.getplatinum.ru/v2/payment/create

# --- модели и кэши ---
# Куда складывать веса Whisper, чтобы не качались заново
WHISPER_CACHE=/opt/nasluh/data/models

# Кэши библиотек. Задаются и в systemd-юните -- здесь для запусков вручную.
# Без них numba внутри librosa падает: писать рядом со своим модулем она
# не может, файловая система для службы открыта только на чтение.
NUMBA_CACHE_DIR=/opt/nasluh/data/cache/numba
XDG_CACHE_HOME=/opt/nasluh/data/cache
