# Распознавание аудио. Ставить ПОСЛЕ requirements.txt:
#     pip install -r requirements-audio.txt
#     pip install --no-deps basic-pitch
#
# Второй командой basic-pitch ставится без разрешения зависимостей намеренно:
# в его метаданных прописан tensorflow<2.15.1, которого нет под Python 3.11+
# на Windows. В коде все бэкенды обёрнуты в try/except, и на ONNX-модели
# tensorflow не используется вообще -- поэтому его отсутствие ничего не ломает.

onnxruntime>=1.16
librosa>=0.10
mir_eval>=0.6
resampy>=0.4
scipy>=1.10
scikit-learn>=1.3
soundfile>=0.12

# ОЖИДАЕМЫЕ предупреждения pip, на которые НЕ надо реагировать:
#   "basic-pitch requires tensorflow<2.15.1 ... which is not installed"
#       -- так и задумано: на ONNX-бэкенде tensorflow не используется;
#   "basic-pitch requires resampy<0.4.3, but you have resampy 0.4.3"
#       -- resampy нужен basic-pitch только для озвучки MIDI обратно в
#          аудио (sonify), наш конвейер эту функцию не вызывает.
# Связка librosa 1.0.0 + resampy 0.4.3 + numpy 2.5.3 на Python 3.12/3.13
# проверена на полном прогоне: аудио -> MIDI -> табы.
